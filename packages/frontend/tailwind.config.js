/** @type {import('tailwindcss').Config} */

const config = {
	content: ["./src/**/*.{js,jsx,ts,tsx}"],
	theme: {
		extend: {},
		fontFamily: {
			display: ["Poppins"],
			body: ["Poppins"],
		},
	},
	plugins: [],
};

export default config;
