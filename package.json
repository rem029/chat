{
  "name": "@chat/chat",
  "description": "Job Dispatch System",
  "private": true,
  "repository": "https://github.com/rem029/chat",
  "scripts": {
    "kill-ports": "kill-port --port 3030 && kill-port --port 6060",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean && rimraf ./node_modules",
    "install": "lerna exec yarn install --scope @chat/*",
    "start": "lerna run start --parallel --scope @chat/*",
    "start:staging": "lerna run build:run --parallel --scope @chat/backend",
    "start:pm2:staging": "pm2 start server.sh --interpreter=bash --name=chat-app-staging",
    "build": "lerna run build --parallel --scope @chat/*",
    "build:frontend": "lerna run build --scope @chat/frontend",
    "build:backend": "lerna run build --scope @chat/backend",
    "lint": "lerna run lint --parallel --scope @chat/*",
    "publish:version": "lerna version --conventional-commits",
    "migrate-latest": "lerna run migrate-latest --scope @chat/backend",
    "migrate-rollback": "lerna run migrate-rollback --scope @chat/backend",
    "cron-add-issue": "pm2 start ./cron/addIssue.js --cron \"0 */12 * * *\" --name=store-add-issue-every-12hr --no-autorestart",
    "add-issue": "node ./cron/addIssue.js"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "kill-port": "^1.6.1",
    "lerna": "^4.0.0",
    "lerna-changelog": "^2.2.0",
    "lerna-wizard": "^1.1.1"
  },
  "changelog": {
    "labels": {
      "feature": "New Feature",
      "fix": "Bug Fix",
      "tech": "Tech"
    }
  },
  "version": "0.0.0"
}
